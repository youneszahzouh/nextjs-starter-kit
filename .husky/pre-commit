#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Default one generated by 'yarn husky install'
# yarn tsc --noEmit && yarn eslint . && yarn prettier --write . 

# Replacing Default behavior with this to check only the staged files
yarn lint-staged
